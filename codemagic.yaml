## ============================================================
## Iqra Academy — Compilation iOS dans le cloud (Codemagic)
## ------------------------------------------------------------
## Ce fichier permet de compiler l'app iOS SANS Mac : Codemagic loue un vrai
## Mac dans son datacenter, y installe le projet, compile, signe et envoie
## le résultat sur TestFlight — automatiquement, à chaque mise à jour.
## Voir GUIDE-PUBLICATION.md, section "Publier sur l'App Store (méthode
## recommandée, sans Mac)" pour la configuration en quelques clics.
## ============================================================
workflows:
  ios-iqra-academy:
    name: Iqra Academy — iOS (TestFlight)
    max_build_duration: 60
    instance_type: mac_mini_m2
    integrations:
      app_store_connect: iqra_academy_asc   # nom donné à l'intégration créée dans Codemagic (Team > Integrations)
    environment:
      ios_signing:
        distribution_type: app_store
        bundle_identifier: com.iqraacademy.app
      vars:
        XCODE_WORKSPACE: "mobile/ios/App/App.xcworkspace"
        XCODE_SCHEME: "App"
        BUNDLE_ID: "com.iqraacademy.app"
      node: latest
      xcode: latest
      cocoapods: default
    triggering:
      events:
        - push
      branch_patterns:
        - pattern: main
          include: true
      cancel_previous_builds: true
    scripts:
      - name: Installer les dépendances (dossier mobile/)
        script: cd mobile && npm ci

      - name: Préparer le web + créer le projet iOS s'il n'existe pas encore
        script: |
          cd mobile
          node build-www.js
          if [ ! -d ios ]; then npx cap add ios; fi
          npx cap sync ios

      - name: Icône de l'app + écran de démarrage (logo Iqra Academy)
        script: |
          cd mobile
          npx capacitor-assets generate --ios --iconBackgroundColor '#0a2e22' --iconBackgroundColorDark '#0a2e22' --splashBackgroundColor '#0a2e22' --splashBackgroundColorDark '#0a2e22'

      - name: Déclarer « pas de chiffrement spécifique » (Apple ne reposera plus la question à chaque build)
        script: |
          PLIST=mobile/ios/App/App/Info.plist
          /usr/libexec/PlistBuddy -c "Add :ITSAppUsesNonExemptEncryption bool false" "$PLIST" || /usr/libexec/PlistBuddy -c "Set :ITSAppUsesNonExemptEncryption false" "$PLIST"

      - name: Installer les dépendances CocoaPods
        script: |
          cd mobile/ios/App
          pod install

      - name: Appliquer la signature sur le projet Xcode (certificat + profil déjà enregistrés dans Codemagic)
        script: |
          xcode-project use-profiles

      - name: Augmenter automatiquement le numéro de build
        script: |
          cd mobile/ios/App
          LATEST=$(app-store-connect get-latest-app-store-build-number "$BUNDLE_ID" || echo 0)
          agvtool new-version -all $(($LATEST + 1))

      - name: Compiler l'app (.ipa)
        script: |
          xcode-project build-ipa \
            --workspace "$XCODE_WORKSPACE" \
            --scheme "$XCODE_SCHEME"

    artifacts:
      - build/ios/ipa/*.ipa
      - /tmp/xcodebuild_logs/*.log

    publishing:
      app_store_connect:
        auth: integration
        submit_to_testflight: false   # le build arrive dans TestFlight : les testeurs INTERNES y ont accès sans review Apple
        submit_to_app_store: false   # tu soumets à la review manuellement depuis App Store Connect quand tu es prêt
      email:
        recipients:
          - daryl.jain@gmail.com   # Codemagic écrit ici si la compilation échoue
